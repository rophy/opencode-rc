{{- if .Values.tunneler.ingress.enabled }}
{{- $defaultAnnotations := dict
  "nginx.ingress.kubernetes.io/proxy-read-timeout" "3600"
  "nginx.ingress.kubernetes.io/proxy-send-timeout" "3600"
-}}
{{- $annotations := merge (.Values.tunneler.ingress.annotations | default dict) $defaultAnnotations -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "opencode-rc.fullname" . }}-tunneler
  labels:
    {{- include "opencode-rc.labels" . | nindent 4 }}
    app.kubernetes.io/component: tunneler
  annotations:
    {{- toYaml $annotations | nindent 4 }}
spec:
  {{- if .Values.tunneler.ingress.className }}
  ingressClassName: {{ .Values.tunneler.ingress.className }}
  {{- end }}
  {{- if .Values.tunneler.ingress.tls }}
  tls:
    {{- toYaml .Values.tunneler.ingress.tls | nindent 4 }}
  {{- end }}
  rules:
    - host: {{ .Values.tunneler.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "opencode-rc.fullname" . }}-tunneler
                port:
                  number: 9090
{{- end }}
