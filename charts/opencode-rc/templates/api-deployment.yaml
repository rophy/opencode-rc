{{- include "opencode-rc.validateProduction" . -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "opencode-rc.fullname" . }}-api
  labels:
    {{- include "opencode-rc.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  replicas: {{ .Values.api.replicas }}
  selector:
    matchLabels:
      {{- include "opencode-rc.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        {{- include "opencode-rc.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: api
        {{- with .Values.api.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.api.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.api.securityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: api
          image: {{ include "opencode-rc.image" (dict "image" .Values.api.image "global" .Values.global) }}
          imagePullPolicy: {{ .Values.api.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: PORT
              value: "8080"
            - name: OIDC_ISSUER
              value: {{ include "opencode-rc.oidcIssuer" . | quote }}
            - name: OIDC_CLIENT_ID
              value: {{ .Values.oidc.clientId | quote }}
            - name: OIDC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "opencode-rc.secretName" . }}
                  key: oidc-client-secret
            - name: OIDC_REDIRECT_URI
              value: {{ include "opencode-rc.redirectUri" . | quote }}
            - name: OIDC_CLI_CLIENT_ID
              value: {{ .Values.oidc.cliClientId | quote }}
            - name: TOKEN_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "opencode-rc.secretName" . }}
                  key: cookie-secret
            - name: COOKIE_SECURE
              value: {{ if eq .Values.profile "production" }}"true"{{ else }}"false"{{ end }}
            {{- /* auth may be absent when upgrading a 0.5.x release with --reuse-values */}}
            {{- $auth := .Values.auth | default dict }}
            - name: ACCESS_TOKEN_TTL
              value: {{ dig "accessTokenTTL" "15m" $auth | quote }}
            - name: SESSION_TTL
              value: {{ dig "sessionTTL" "7d" $auth | quote }}
            {{- $origins := dig "allowedOrigins" list $auth }}
            {{- if .Values.ui.enabled }}
            {{- $origins = prepend $origins (include "opencode-rc.uiPublicUrl" .) }}
            {{- end }}
            {{- with $origins }}
            - name: ALLOWED_ORIGINS
              value: {{ join "," . | quote }}
            {{- end }}
            {{- if .Values.oidc.issuerOverride }}
            - name: OIDC_ISSUER_OVERRIDE
              value: {{ .Values.oidc.issuerOverride | quote }}
            {{- end }}
            {{- if .Values.oidc.authorizationEndpoint }}
            - name: OIDC_AUTHORIZATION_ENDPOINT
              value: {{ .Values.oidc.authorizationEndpoint | quote }}
            {{- end }}
            {{- if .Values.oidc.tokenEndpoint }}
            - name: OIDC_TOKEN_ENDPOINT
              value: {{ .Values.oidc.tokenEndpoint | quote }}
            {{- end }}
            {{- if .Values.oidc.jwksUri }}
            - name: OIDC_JWKS_URI
              value: {{ .Values.oidc.jwksUri | quote }}
            {{- end }}
            {{- if .Values.tlsInsecureSkipVerify }}
            - name: TLS_INSECURE_SKIP_VERIFY
              value: "true"
            {{- end }}
            {{- if .Values.extraCACerts }}
            - name: NODE_EXTRA_CA_CERTS
              value: "/etc/ssl/certs/extra/ca-bundle.crt"
            {{- end }}
            {{- if .Values.redis.enabled }}
            - name: REDIS_URL
              value: {{ include "opencode-rc.redisUrl" . | quote }}
            {{- else }}
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "opencode-rc.secretName" . }}
                  key: redis-url
            {{- end }}
            - name: GATEWAY_URL
              value: "http://{{ include "opencode-rc.fullname" . }}-gateway:9090"
          {{- with .Values.api.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if .Values.extraCACerts }}
          volumeMounts:
            - name: extra-ca-certs
              mountPath: /etc/ssl/certs/extra
              readOnly: true
          {{- end }}
          {{- with .Values.api.containerSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- if .Values.extraCACerts }}
      volumes:
        - name: extra-ca-certs
          configMap:
            name: {{ include "opencode-rc.fullname" . }}-extra-ca-certs
      {{- end }}
      {{- with .Values.api.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.api.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.api.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
